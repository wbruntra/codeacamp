<%= erb :_top, locals: {:user_id => session['user_id']} %>
  <h1>Quora!</h1>
<% for question in @questions %>
  <%= erb :_displayquestion, locals: { :question => question, :user_id => session['user_id'] } %>
<% end %>
